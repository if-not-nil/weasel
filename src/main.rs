// use lung::shared::crypt::go_through;

fn main() {
    // go_through();
    // let tok = lung::server::auth::Token::new("pebis".to_string(), 28);
    // let cipheretext = tok.encrypt(&derive_key("yo"));
    // println!("{:?}", shared)
    // Server::new("0.0.0.0:1337").listen();
}
